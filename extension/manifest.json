{
  "manifest_version": 2,
  "name": "Disable CORS",
  "version": "1.0.3",
  "description": "Zero configuration, open source extension to disable CORS everywhere.",
  "background": {
    "scripts": ["background.js"]
  },
  "permissions": [
    "webRequest",
    "webRequestBlocking",
    "<all_urls>"
  ]
}
